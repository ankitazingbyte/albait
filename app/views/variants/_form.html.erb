<%= form_for @variant do |form| %>
  <% if variant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(variant.errors.count, "error") %> prohibited this variant from being saved:</h2>

      <ul>
      <% variant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :yes %>
    <%= form.text_field :yes, id: :variant_yes %>
  </div>

  <div class="field">
    <%= form.label :no %>
    <%= form.text_field :no, id: :variant_no %>
  </div>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.number_field :product_id, id: :variant_product_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
